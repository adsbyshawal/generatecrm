// Get Started / Book a Demo page (/get-started) — scaffold stub.
// Hosts the LeadConnector booking iframe (primary conversion endpoint) once built.
export default function GetStartedPage() {
  return null;
}
